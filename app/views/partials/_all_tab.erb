<ul class="book-shelf all">
  <% @user.shelves.find_by(title: 'all').books.each do |book| %>
  <li class="book">
    <p class="all-title"><%= book.title %></p>
    <p class="all-author"><%= book.author %></p>
  </li>
  <% end %>
</ul>
