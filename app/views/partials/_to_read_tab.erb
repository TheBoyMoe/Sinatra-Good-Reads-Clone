<ul class="book-shelf to-read">
  <% @user.shelves.find_by(title: 'to-read').books.each do |book| %>
  <li class="book">
    <p class="to-read-title"><%= book.title %></p>
    <p class="to-read-author"><%= book.author %></p>
  </li>
  <% end %>
</ul>
