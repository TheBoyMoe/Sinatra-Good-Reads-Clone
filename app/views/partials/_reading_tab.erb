<ul class="book-shelf reading">
  <% @user.shelves.find_by(title: 'reading').books.each do |book| %>
  <li class="book">
    <p class="reading-title"><%= book.title %></p>
    <p class="reading-author"><%= book.author %></p>
  </li>
  <% end %>
</ul>
